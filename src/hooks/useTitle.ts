export const useTitle = () => {
  let getTitle = (props, arr) => {
    // let { seekerData } = props
    // let node = seekerData.jobSpecific?.split('-').pop()
    // return [`${seekerData.info?.age} 岁`, node, seekerData.info?.gongZuoJingYan]
    //   .filter((el) => !!el)
    //   .join(' • ')
  }
  return { getTitle }
}
